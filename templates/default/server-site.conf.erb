server {
       listen <%= @server_port %>;
       server_name <%= @server_hostname %>;

       root <%= @server_root %>;
       index index.html;

       location / {
               try_files $uri $uri/ =404;
       }
       <%= "return 301 https://$host$request_uri;" if @https_enabled %>
}